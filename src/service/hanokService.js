const userDao = require('../dao/hanokDao');

async function getHanokMap(userData) {
    // return await userDao.insertUser(userData);
}

async function getHanokList(userData) {
    // return await userDao.insertUser(userData);
}

async function getHanokDetail(userData) {
    // return await userDao.insertUser(userData);
}

async function postHanokReservation(userData) {
    // return await userDao.insertUser(userData);
}

module.exports = {
    getHanokMap,
    getHanokList,
    getHanokDetail,
    postHanokReservation
}